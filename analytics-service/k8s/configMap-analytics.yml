apiVersion: v1
kind: ConfigMap
metadata:
  name: analytics-config
  namespace: analytics
data:
  PORT: "8005"
  # Preencha com a URL da fila SQS e a região (obrigatório para o worker)
  AWS_SQS_URL: "${__SQS_QUEUE_URL__}"
  AWS_DYNAMODB_TABLE: "ToggleMasterAnalytics"
  AWS_REGION: "us-east-1"