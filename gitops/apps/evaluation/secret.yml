apiVersion: v1
kind: Secret
metadata:
  name: evaluation-secret
  namespace: togglemaster
type: Opaque
stringData:
  # Defina SERVICE_API_KEY no .env (ou gere no auth e re-renderize).
  SERVICE_API_KEY: ${SERVICE_API_KEY}

