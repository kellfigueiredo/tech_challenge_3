apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: togglemaster
  labels:
    app.kubernetes.io/name: analytics
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: analytics
  ports:
    - port: 80
      targetPort: 80
      name: http
