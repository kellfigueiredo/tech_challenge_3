# ConfigMap gerenciado pelo Terraform (kubernetes_config_map.analytics)
# Endpoints de infraestrutura (SQS, DynamoDB) são injetados dinamicamente
# a partir dos outputs do Terraform, sem necessidade de edição manual.
#
# Chaves gerenciadas: PORT, AWS_SQS_URL, AWS_DYNAMODB_TABLE, AWS_REGION
