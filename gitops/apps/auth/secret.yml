# Secret gerenciado pelo Terraform (kubernetes_secret.auth_service)
# Não commitar senhas — os valores reais são injetados via pipeline
# usando a variável TF_VAR_DB_PASSWORD dos GitHub Secrets.
#
# Chaves gerenciadas: DATABASE_URL, MASTER_KEY
