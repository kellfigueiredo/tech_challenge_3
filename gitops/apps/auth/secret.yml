apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: togglemaster
type: Opaque
stringData:
  # Ajuste usuário/senha/host ao seu Postgres no namespace auth (ex.: Service postgres)
  # Preencha RDS_* no .env e rode scripts/render-k8s-manifests.sh (envsubst usa ${...}).
  DATABASE_URL: postgres://toggleadmin:D37eas4510458845@togglemaster-dev-auth.c1ir6mjchota.us-east-1.rds.amazonaws.com:5432/toggledb
  MASTER_KEY: admin-secreto-123


