# ConfigMap gerenciado pelo Terraform (kubernetes_config_map.evaluation)
# Endpoints de infraestrutura (Redis, SQS) são injetados dinamicamente
# a partir dos outputs do Terraform, sem necessidade de edição manual.
#
# Chaves gerenciadas: PORT, REDIS_URL, FLAG_SERVICE_URL, TARGETING_SERVICE_URL, AWS_REGION, AWS_SQS_URL
